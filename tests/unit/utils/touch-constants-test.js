import touchConstants from '../../../utils/touch-constants';
import { module, test } from 'qunit';

module('touchConstants');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = touchConstants();
  assert.ok(result);
});
